{
  "name": "tasksheet-orchestrator",
  "version": "1.0.0",
  "private": true,
  "scripts": {
  "dev": "vite",
  "build": "vite build",
  "preview": "vite preview",
  "postinstall": "npm install --prefix server",
  "start": "npm --prefix ./server start"
},
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
